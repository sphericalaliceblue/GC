// ============================================================
// Test.gs — 手動テスト用関数
// GASの「実行」ボタンから手動で呼び出して動作確認に使う
// ============================================================

/**
 * Dify連携のテスト
 * 自然文をDifyに送り、タスク情報として正しく解析されるか確認する
 */
function testDify() {
  const result = callDify('４月５日の発表用の資料を前日までに作る');
  console.log('Dify結果:', JSON.stringify(result));
}
